# Changesets

This directory holds changesets for versioning and publishing. Run `pnpm changeset` after a change that should ship a release, and answer its prompts to pick affected packages and a bump type. See https://github.com/changesets/changesets for the full docs.
